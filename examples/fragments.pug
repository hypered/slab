fragment list
  ul
    li foo
    li bar
    li baz

fragment post
  h1 Post.
  block content

fragment card
  h1 Card.
  content

body
  list

  post

  post
    frag content
      p Some content.

  //- Note that this doesn't pass `content`, but instead pass `block content`
  //- as an unnamed fragment. This results in twice `block content` in the AST.
  card
    block content
      p Some content.

  card
    frag content
      p Some content.

  card
    p Some content.
